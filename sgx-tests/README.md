# SGX tests for ripple-keypairs

## Prerequisites

* [Rust SGX SDK](https://github.com/apache/incubator-teaclave-sgx-sdk)

## Running the tests

With the SDK configured, this should work:

```
make
make run
```
